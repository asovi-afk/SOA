syntax = "proto3";

option go_package = "analytics/notify";

package notify;

service Notifier {
    // client-side streaming RPC
    rpc Notify (stream NotificationRequest) returns (NotificationResponse) {}
}

message NotificationRequest {
    string title = 1;
}

message NotificationResponse {
    string msg = 1;
}